# disabled because it really fucks shit up
[toolchain]
# this is just the version i last used, anything since ~2022 should work
channel = "nightly-2024-12-21"
components = ["rust-src", "miri"]
profile = "minimal"
